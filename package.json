{
  "scripts": {
    "start": "sls offline --httpPort 4000",
    "deploy": "sls deploy",
    "destroy": "sls remove",
    "test": "jest --silent"
  },
  "dependencies": {
    "axios": "^0.21.0",
    "yarn": "^1.22.10"
  },
  "devDependencies": {
    "jest": "^26.6.3",
    "serverless": "2.11.1",
    "serverless-offline": "^6.8.0"
  }
}
